import React from 'react';

const Catalog = (props) => (
  <div style={styles.container}>
    Catalog
  </div>
);

const styles = {
  container: {
    width: 200,
    background: '#888',
  }
};
export default Catalog;